# ============================================
# CTSS Environment Variables cho Hostinger
# ============================================
# Copy file này thành .env và điền thông tin thực tế
# cp env.hostinger.template .env
# nano .env

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Lấy thông tin từ Hostinger hPanel → Databases
# Hoặc nếu tự cài PostgreSQL trên VPS:
#   Host: localhost
#   Port: 5432
#   Database: ctss_db
#   Username: ctss_user
#   Password: your-secure-password
DATABASE_URL="postgresql://ctss_user:your-password@localhost:5432/ctss_db?schema=public"

# ============================================
# NEXT.JS CONFIGURATION
# ============================================
# Môi trường: production cho server, development cho local
NODE_ENV=production

# URL công khai của ứng dụng (sau khi setup domain)
# Nếu chưa có domain, dùng IP: http://your-vps-ip:3000
# Nếu có domain: https://your-domain.com
NEXT_PUBLIC_APP_URL=https://your-domain.com

# ============================================
# JWT SECRET (BẮT BUỘC)
# ============================================
# Tạo một chuỗi ngẫu nhiên mạnh cho JWT
# Có thể dùng: openssl rand -base64 32
# HOẶC: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# ============================================
# OPENAI API (TÙY CHỌN - Nếu dùng tính năng AI)
# ============================================
# Lấy từ: https://platform.openai.com/api-keys
# Format: sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Model mặc định: gpt-4o-mini (rẻ) hoặc gpt-4o (mạnh hơn)
OPENAI_MODEL=gpt-4o-mini

# ============================================
# PORT (TÙY CHỌN)
# ============================================
# Port mặc định của Next.js là 3000
# Chỉ cần set nếu muốn dùng port khác
# PORT=3000

# ============================================
# NOTES
# ============================================
# 1. KHÔNG commit file .env lên Git
# 2. Đảm bảo DATABASE_URL đúng format PostgreSQL
# 3. JWT_SECRET phải là chuỗi ngẫu nhiên mạnh
# 4. NEXT_PUBLIC_APP_URL phải match với domain/IP thực tế
# 5. Sau khi cập nhật .env, cần restart app: pm2 restart ctss
